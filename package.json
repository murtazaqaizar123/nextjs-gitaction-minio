{
  "name": "shopping-cart",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "NODE_OPTIONS=--openssl-legacy-provider next build",
    "start": "next start",
    "export": "next export",
    "build:static": "NODE_OPTIONS=--openssl-legacy-provider next build && next export"

},
  "dependencies": {
    "@reduxjs/toolkit": "^1.6.0",
    "next": "11.0.0",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-redux": "^7.2.4"
  }
}
